// Since pgbranch is the command and the name of the binary being called, what will it's
//
// All 3 could do it without duplication only if there are no pgbranch.rs and pgroll.rs, only as you say, a docker.rs (to replace load.rs).
//
